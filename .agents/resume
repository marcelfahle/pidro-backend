#!/usr/bin/env bash
set -euo pipefail

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$repo_root"

if ! pg_isready --host 127.0.0.1 --port 5432 --quiet; then
  printf 'Starting the supervised PostgreSQL service...\n'
  amp orb services ensure
fi
