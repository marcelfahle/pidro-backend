== Dev Panel Overhaul Progress ==

--- 2026-02-09 — Feature 1.2: Fix BotManager.start_bots seat-skipping ---
Status: DONE
Changed: apps/pidro_server/lib/pidro_server/games/bots/bot_manager.ex

What was done:
- Rewrote start_bots/4 to query RoomManager.get_room(room_code) and use
  Positions.available(room) to get only empty seats, instead of blindly
  assigning from [:north, :east, :south, :west].
- Returns {:error, :not_enough_seats} when fewer empty seats than bot_count.
- Dependency direction: BotManager -> RoomManager (read-only), which is correct.

Why this was prioritized first:
- Functional bug fix — bots were overwriting occupied seats, causing data
  corruption. This blocks several Phase 2 presets (1H+3B, 2H+2B) from
  working correctly.

Note for next person:
- mix format and mix test were NOT run due to permission issues in the
  automated session. Please verify manually before pushing:
    mix format --check-formatted
    mix test
- Feature 1.1 (delete admin views) and 1.3 (cascading cleanup) are the
  natural next picks. 1.1 is pure deletion/cleanup; 1.3 adds functional
  safety for room deletion flows.

--- 2026-02-09 — Feature 1.3: Cascading cleanup on room deletion ---
Status: DONE
Changed:
  - apps/pidro_server/lib/pidro_server_web/live/dev/game_list_live.ex
  - apps/pidro_server/lib/pidro_server_web/live/dev/game_detail_live.ex

What was done:
- GameListLive.handle_single_delete: added BotManager.stop_all_bots(room_code)
  and GameSupervisor.stop_game(room_code) before RoomManager.close_room.
- GameListLive.handle_bulk_delete: same cascading cleanup for each finished
  room before closing it.
- GameDetailLive.handle_event("assign_seat"): added BotManager.list_bots
  check — if a bot occupies the target position, calls BotManager.stop_bot
  before reassigning the seat.
- Added GameSupervisor alias to GameListLive imports.

Why this was prioritized:
- Functional safety — deleting a room without stopping bots/game leaves
  orphaned processes (bot GenServers, game process) that leak memory and
  can cause errors on subsequent room creation with the same code.
- Reassigning a bot's seat without stopping it left the bot process running
  against a seat it no longer owns, causing ghost actions.

Note for next person:
- mix format and mix test were NOT run due to permission issues in the
  automated session. Please verify manually before pushing:
    mix format --check-formatted
    mix test
- Next picks: 1.1 (delete admin views — pure cleanup), 1.4 (dead code
  removal), or 2.1 (quick-create presets — builds on 1.2 + 1.3).

--- 2026-02-09 — Feature 1.1: Delete admin views and routes ---
Status: DONE (was already completed in working tree by previous session)
Changed:
  - Deleted: apps/pidro_server/lib/pidro_server_web/live/lobby_live.ex
  - Deleted: apps/pidro_server/lib/pidro_server_web/live/game_monitor_live.ex
  - Deleted: apps/pidro_server/lib/pidro_server_web/live/stats_live.ex
  - Modified: apps/pidro_server/lib/pidro_server_web/router.ex (admin scope removed)

What was done:
- Three admin LiveViews deleted: LobbyLive, GameMonitorLive, StatsLive.
- /admin scope and admin_basic_auth plug removed from router.ex.
- No admin-specific test files existed (grep confirmed).
- Remaining references to admin views only exist in docs/thoughts files
  (MASTERPLAN, README, etc.) — not in source code.
- admin_basic_auth config was already absent from config/dev.exs.

--- 2026-02-09 — Feature 1.4: Clean up dead code in GameDetailLive ---
Status: DONE
Changed: apps/pidro_server/lib/pidro_server_web/live/dev/game_detail_live.ex

What was done:
- Removed 79-line commented-out position buttons block (lines ~990-1068).
  These buttons were replaced by click-on-player-name in the game table.
- Audited all private helper functions for unused code after admin view
  deletion. All helpers (format_action, get_score_history, status_color,
  is_seat_available?, etc.) are still actively referenced in the template
  or handle_event clauses — no unused helpers to remove.
- Net reduction: ~79 lines of dead commented-out code removed.

Note for next person:
- mix format and mix test were NOT run due to permission issues in the
  automated session. Please verify manually before pushing:
    mix format --check-formatted
    mix test
- Phase 1 remaining: only 1.5 (tests). Phase 2 features (quick-create
  presets) are the natural next high-impact picks, especially 2.1
  (Empty Room + 1H+3B presets) which builds on 1.2 + 1.3.

--- 2026-02-09 — Feature 2.1: Quick-Create Presets (Empty Room + 1H+3B) ---
Status: DONE
Changed: apps/pidro_server/lib/pidro_server_web/live/dev/game_list_live.ex

What was done:
- Added "Quick Create" section above the existing "Create New Game" form with
  two one-click preset buttons: "Empty Room" and "1H + 3B".
- "Empty Room" preset: creates a room with a random dev host name at north,
  no bots, 3 empty seats. Navigates to the detail view on success.
- "1H + 3B" preset: creates a room with dev host at north, then calls
  start_bots_if_needed(room.code, 3, "random") to fill 3 remaining seats.
  On bot failure, calls BotManager.stop_all_bots for cleanup. Navigates to
  the detail view regardless.
- Added random_suffix/0 helper that generates 6-char hex string via
  :crypto.strong_rand_bytes(3) for unique auto-generated room names.
- Both presets use push_navigate to redirect to /dev/games/:code after
  creation, as specified in the PRD.

Why this was prioritized:
- Highest UX impact for dev workflow — eliminates multiple form interactions
  for the two most common room configurations (empty room for manual testing,
  full room with bots for automated play observation).
- Builds directly on 1.2 (seat-skipping fix) and 1.3 (cascading cleanup),
  which are prerequisites for reliable bot assignment.

Note for next person:
- mix format and mix test were NOT run due to permission issues in the
  automated session. Please verify manually before pushing:
    mix format --check-formatted
    mix test
- Next picks: 2.2 (2H+2B and 4 Bots presets — more complex seat assignment),
  2.3 (phase/scores columns in room table), or 3.1 (tab infrastructure for
  GameDetailLive). 2.2 builds directly on this work.

--- 2026-02-09 — Feature 2.2: Quick-Create Presets (2H+2B + 4 Bots) ---
Status: DONE
Changed: apps/pidro_server/lib/pidro_server_web/live/dev/game_list_live.ex

What was done:
- Added "2H + 2B" preset button: creates room with dev host at north, starts
  bots at specifically east and west positions (using new start_bots_at_positions
  helper), leaves south open for a second human player.
- Added "4 Bots" preset button: creates room, clears host from north via
  dev_set_position(code, :north, nil), then calls start_bots(code, 4, :random)
  to fill all 4 seats with bots. This avoids the race condition described in the
  PRD — clearing the host first ensures all 4 seats are available before bots
  start joining.
- Added start_bots_at_positions/3 private helper that calls BotManager.start_bot
  for each specific position (unlike start_bots which fills first-N available).
  Includes try/catch for :noproc errors when BotManager isn't running.
- Both presets call BotManager.stop_all_bots on failure for cleanup.
- Both presets navigate to the detail view after creation.
- Buttons styled consistently: blue for 2H+2B, purple for 4 Bots.

PRD deviation note:
- The PRD suggested using dev_set_position to pre-assign bot IDs before starting
  processes. Investigation revealed this doesn't work — BotPlayer.init calls
  join_room which returns {:error, :already_seated} if the ID is already in the
  positions map. Instead, for 4 Bots: we clear the host seat, then start_bots
  fills all 4 empty positions normally.

Verification:
- mix format --check-formatted passes (also fixed pre-existing format issues in
  6 other files: config/config.exs, config/test.exs, play.ex, and 3 test files)
- mix test passes (engine: 529 tests, 170 properties, 79 doctests; 1 pre-existing
  flaky failure in SupervisorTest.lookup_game/1, 4 pre-existing skips)

Note for next person:
- Phase 2 remaining: 2.3 (phase/scores columns in room table).
- Phase 1 remaining: 1.5 (tests for bot seat-skipping and cascading cleanup).
- Phase 3 (tabbed detail view) is the next major chunk — 3.1 adds tab
  infrastructure, then 3.2-3.5 extract content into tabs.

--- 2026-02-09 — Feature 3.1: Tab infrastructure for GameDetailLive ---
Status: DONE
Changed: apps/pidro_server/lib/pidro_server_web/live/dev/game_detail_live.ex

What was done:
- Added @active_tab assign (default :board) in mount/3.
- Added handle_params/3 to sync @active_tab with URL query param ?tab=board
  or ?tab=seats_bots. Uses .link patch for tab switching (no full page reload).
- Extracted defp render_tab_bar/1 — horizontal tab navigation bar with "Board"
  and "Seats & Bots" tabs, styled with indigo underline for active tab.
- Restructured render/1 into: header + room info card + tab bar + conditional
  tab content + auto-refresh indicator. render/1 is now ~65 lines (down from
  ~838 lines of template).
- Extracted defp render_board_tab/1 — contains all existing board content:
  position selector, quick actions, replay controls, card table (split/single
  view + game over overlay), legal actions panel, game state card, event log,
  export modal, and raw state JSON collapsible.
- Extracted defp render_seats_tab/1 — new tab with bot configuration (migrated
  from Board tab's collapsible) and seat management panel with per-position
  user assignment dropdowns.
- Bot configuration on the Seats & Bots tab is now a full section (not a
  collapsible details element), making it more prominent.
- All existing handle_event and handle_info clauses preserved unchanged — no
  logic changes.

Why this was prioritized:
- Unblocks all of Phase 3 (3.2-3.5). The tabbed interface is the core
  architectural refactor of the project — every subsequent feature in Phase 3
  depends on this infrastructure being in place.
- The monolithic render/1 at ~838 lines was the largest single function in the
  codebase and was difficult to navigate or modify safely.

Verification:
- mix format --check-formatted passes
- mix compile succeeds with only pre-existing warnings (3 unused bid-helper
  functions, replay_controller, room_schemas)
- mix test: engine 529 tests/170 properties/79 doctests all pass; server
  tests have 5 pre-existing failures (supervisor race, lobby channel format,
  game channel timeout) — none related to this change

Note for next person:
- Phase 3 remaining: 3.2 (extract board tab content — mostly done since
  render_board_tab already exists, but PRD wants further refinement), 3.3
  (extract seats tab content — seats_tab already has basic content), 3.4
  (Take a Seat shortcut on Board tab), 3.5 (god mode with manual position
  selection).
- The render/1 is ~65 lines — not yet under 50 as the PRD targets. This will
  happen when 3.2 and 3.3 further refine the tab content.
- Phase 2 remaining: 2.3 (phase/scores columns in room table).
- Phase 1 remaining: 1.5 (tests).

--- 2026-02-09 — Feature 2.3: Phase and scores columns in room table ---
Status: DONE
Changed: apps/pidro_server/lib/pidro_server_web/live/dev/game_list_live.ex

What was done:
- Added "Phase" and "Score" columns to the room table between Players and Host.
- Phase column shows human-readable game phase (e.g., "Trick 3", "Bidding",
  "Complete") for rooms with active/finished games. Shows "-" for waiting rooms.
- Score column shows cumulative N/S vs E/W scores in monospace font (e.g.,
  "12 - 8") for rooms with active/finished games. Shows "-" for waiting rooms.
- Added GameAdapter alias and fetch_game_states/1 helper that queries game state
  only for rooms in :playing or :finished status (avoids unnecessary lookups for
  waiting rooms).
- Added format_game_phase/1 and format_scores/1 private helpers for display
  formatting. Phase display includes trick number during play phase.
- Game states are stored in @game_states assign (map of room_code => game_state)
  and refreshed on every room list update.

Why this was prioritized:
- Last remaining Phase 2 feature — completing Phase 2 entirely.
- High-value UX improvement: at a glance, the dev can see which games are in
  which phase and what the scores are without clicking into each room.
- Self-contained change with no dependencies on incomplete features.

Verification:
- mix compile succeeds (only pre-existing warnings)
- mix format --check-formatted passes
- mix test: engine 529 tests/170 properties/79 doctests pass; server tests
  have 5 pre-existing failures (supervisor race, lobby channel format) — none
  related to this change

Note for next person:
- Phase 2 is now COMPLETE (2.1, 2.2, 2.3 all done).
- Phase 1 remaining: 1.5 (tests for bot seat-skipping and cascading cleanup).
- Phase 3 remaining: 3.2 (extract board tab content), 3.3 (extract seats tab),
  3.4 (Take a Seat shortcut), 3.5 (god mode with position selection).
- The render/1 in GameDetailLive is ~65 lines — Phase 3.2/3.3 should get it
  under 50.

--- 2026-02-09 — Feature 3.4: Take a Seat shortcut on Board tab ---
Status: DONE
Changed: apps/pidro_server/lib/pidro_server_web/live/dev/game_detail_live.ex

What was done:
- Added a "Take a Seat" card on the Board tab (in render_board_tab) between the
  Position Filter section and the Dev Quick Actions section.
- The card contains an inline form with:
  - Position dropdown (North/East/South/West) showing (occupied)/(empty) status
  - User dropdown (same user list as the Seats & Bots tab) with a "-- Clear seat --" option
  - "Assign" submit button
- The form submits to the existing "assign_seat" event handler — no new logic
  was needed. This handler already handles bot eviction (stops bot before
  reassigning), seat clearing, and user assignment.
- This gives devs a quick way to assign seats without switching to the Seats &
  Bots tab, which is especially useful when observing a game in progress.

Why this was prioritized:
- High-impact UX shortcut — during active game observation on the Board tab,
  having to switch tabs just to assign a seat breaks flow. This keeps the dev
  on the Board tab for the most common seat operations.
- Zero risk — reuses existing event handler with no new backend logic.
- Simple, self-contained change (single file, ~40 lines of template).

Verification:
- mix format --check-formatted passes
- mix compile succeeds (only pre-existing warnings)
- mix test: engine 529 tests/170 properties/79 doctests pass; 1 pre-existing
  flaky failure in SupervisorTest.lookup_game/1; 4 pre-existing skips

Note for next person:
- Phase 3 remaining: 3.2 (extract board tab — PRD says further refinement but
  render_board_tab already exists from 3.1), 3.3 (extract seats tab — same
  situation), 3.5 (god mode with manual position selection).
- Phase 1 remaining: 1.5 (tests for bot seat-skipping and cascading cleanup).
- The render/1 in GameDetailLive is ~65 lines. Phase 3.2/3.3 should get it
  under 50 as the PRD targets.

--- 2026-02-09 — Features 3.2, 3.3, 3.5: Complete Phase 3 tab refactoring ---
Status: DONE
Changed: apps/pidro_server/lib/pidro_server_web/live/dev/game_detail_live.ex

What was done:
- Feature 3.3 (render/1 under 50 lines): Extracted the inline Room Info Card
  from render/1 into defp render_room_info/1. render/1 is now 36 lines (down
  from ~65), well under the PRD target of 50.
- Feature 3.2 (extract board tab): Already completed in 3.1 — render_board_tab/1
  was created during the tab infrastructure work. Verified it contains all board
  content: position selector, quick actions, replay controls, card table, legal
  actions panel, game state card, event log, export modal, raw state JSON.
- Feature 3.5 (god mode with position selection): Already fully implemented in
  prior work. Verified all PRD requirements:
  - selected_position defaults to :all on mount (line 65)
  - Clicking position names switches perspective via select_position event handler
  - God Mode toggle button on card_table returns to :all view
  - Current turn highlighted with green ring, green background, and pulsing
    "← Turn" indicator

Why these were prioritized together:
- 3.2 and 3.5 were already implemented but not marked as passing in the PRD.
  Verified and marked them done.
- 3.3 only needed the Room Info Card extraction to meet its target (render/1
  under 50 lines). This was a minimal, safe refactoring.

Verification:
- mix format --check-formatted passes
- mix compile succeeds (only pre-existing warnings)
- mix test: engine 529 tests/170 properties/79 doctests pass; 0 failures;
  4 pre-existing skips

Note for next person:
- Phase 3 is now COMPLETE (3.1, 3.2, 3.3, 3.4, 3.5 all done).
- Phase 2 is COMPLETE (2.1, 2.2, 2.3 all done).
- Phase 1 remaining: only 1.5 (tests for bot seat-skipping and cascading
  cleanup). This is the LAST remaining feature in the entire PRD.

--- 2026-02-09 — Feature 1.5: Tests for bot seat-skipping and cascading cleanup ---
Status: DONE
Changed:
  - Created: apps/pidro_server/test/pidro_server/games/bots/bot_manager_test.exs

What was done:
- Created BotManagerTest with 9 tests across 3 describe blocks:
  1. start_bots/4 skips occupied seats (3 tests):
     - Host at north, request 3 bots → bots placed at east/south/west
     - Host + player at east, request 2 bots → bots at south/west
     - 3 seats occupied, request 1 bot → fills the only available seat (west)
  2. start_bots/4 returns error when not enough seats (2 tests):
     - Host at north, request 4 bots → {:error, :not_enough_seats}
     - 3 seats occupied, request 2 bots → {:error, :not_enough_seats}
  3. Cascading cleanup via stop_all_bots (4 tests):
     - After stop_all_bots, list_bots returns empty map
     - stop_all_bots on room with no bots returns :ok
     - stop_all_bots terminates bot processes (checks Process.alive?)
     - Full cascading cleanup: stop bots then close room (same pattern
       as GameListLive.handle_single_delete)
- Tests use async: false (singleton GenServers), same setup pattern as
  RoomManagerTest (check GenServer.whereis, start_supervised! if needed,
  reset_for_test).
- All bot delay set to 0ms for fast test execution.

Verification:
- mix format --check-formatted passes
- mix test: all 9 new tests pass; engine 529/170/79 pass (1 pre-existing
  flaky SupervisorTest); server 200 tests with 5 pre-existing failures
  (LobbyChannelTest format, GameChannelTest reconnection) — none related
  to this change.

=== PRD STATUS: ALL FEATURES COMPLETE ===
- Phase 1: 1.1, 1.2, 1.3, 1.4, 1.5 — all DONE
- Phase 2: 2.1, 2.2, 2.3 — all DONE
- Phase 3: 3.1, 3.2, 3.3, 3.4, 3.5 — all DONE
